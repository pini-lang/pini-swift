# Pini spec 证据表（Evidence Ledger）
#
# 治理协议：spec §1.4 证据新鲜度协议（Evidence Freshness）
# - 粒度：60 分钟。证据自 validated_at 起 60 分钟内可信；超期未重新验证 → status = "STALE"（不可信）。
# - 过筛不成即不可信：治理时 grep/read 对应 code_ref——找不到符号、或语义不符 → STALE，
#   不得作为写入依据。机械过筛（行号存在/符号存在）≠ 可信；语义验证才算 FRESH。
# - 行号 = 软证据（软定位）：code_ref 中的行号仅作快速查看指导，不参与 FRESH/STALE 判定。
#   注释/编辑会移动行号（spec §A 出处曾整体漂移 +120~180 行而语义未变）；权威判定键 = 符号名 + 语义。
#   行号漂移而符号仍在附近 → 证据仍可信；治理时可顺手更新行号（见各条目 note 中的「code_ref 自 X 修正」）。
# - 刷新协议（MUST）：把待刷新的 STALE 条目拷贝到新条目（保留痕迹），移除旧条目，
#   重新验证后写入新 validated_at。每次治理只保留一张当前证据表（本文件）。
# - status 取值：FRESH（本次治理 ≤60min 内验证通过）| STALE（超期 / 验证失败 / 语义已过时）。
# - 约定：code_ref 行号为软证据；若 spec 正文仍引用旧行号，在 note 中标注，
#   由治理方按 code_ref 同步修正 spec 正文（§A 出处块行号系统性漂移，见本次 refresh_note）。
#
# 本表由治理脚本/人工在批次治理时更新；spec 正文的代码引用以符号定位为主、行号为软证据（§1.4）。

[meta]
spec_ref = "pini-spec-v0.md §1.4"
granularity_minutes = 60
scan_exclude = ["docs/spec/issue/issue-evidence-table-sweep-2026-09-03.md"]
last_refresh = "2026-09-04T17:55+08:00"
last_sweep = "2026-09-04T09:02:00+08:00"
dangling_ids = ["E-035", "E-047"]
refresh_note = "2026-08-25 00:26 文档治理（用户决策，第三轮）：spec 内部自相矛盾消除——依据 Pini草稿.md 意图：① 块标签统一为 `scope 块标签:`（§2.4 / §A.1.4 / §A.2.4 / §A.5.1，`#label` 与 `@` sigil 标注为历史）；② match 通配统一为 `case _:`（§A.2.4 wildcard-block 删除、规则 3.7/3.10 改写）；③ struct-decl 组合改为可选；④ `^` 右值糖补入 unary-expr/层 8；⑤ IDENT 改 Unicode 字符类；⑥ §3 清单条目标注『语法已定义（§A 出处）/运行语义未定』；⑦ 新增『已闭环缺口索引』。E-042/E-044/E-051 随 spec 修正转 FRESH。前两轮（00:15-00:20）结论保留：行号软证据化 + §2.6 诊断码权威映射落地；2026-08-26 Phase 0 治理（v0.48.0）：补 E-052/053/054（规则 3.13/3.14/3.15 待实现，STALE）；E-008 方法缺省假定随规则 3.2 逆转置 STALE；E-047 关键字数标注待 Phase 1/2 对齐（spec §A.1.1 称 33，代码现 lex 30）；2026-08-27 Phase 2b 解释器落地（ADR-017）：新增 E-057/E-058/E-059/E-060（SystemDL/FFILoader/ForeignThunk/FileLoader[ffi]），E-056 扩展（E5-015 注册期 fail-fast 被 dlsym E5-017 取代）；987 XCTest 0 失败。2026-09-02 批次治理（批 1.5 / 2 / 3 证据补登记）：新增 E-067..E-081——G55 跨行字面量的括号栈与块携带判定（宿主 + 自举镜像）、G56 下标三通道（读分派/E5-005/类型推断/成员登记/解释器分派与 UB 近似/字典键匹配/字典成员派发/LLVM bk_array_get）、G57 `=` 记法（实参/元组/字典/match 保留 `:`/自举同步）；全部于登记前现跑按符号重筛，status=FRESH。E-076 为源码级证据（本环境无 lli，未实跑，按 ADR-024 D9 如实标注）。2026-09-02 F5 钉定：新增 E-082..E-086（六探针实测 + 差分实验 + 记号层不可见性），并裁决 spec 维护注自相矛盾句为「空行透明」（F5 提议『空行闭块』假设被否定）。2026-09-02 批 5（G58 IO 路径基准）：新增 E-087..E-090——三段式解析、moduleRoot 与 CLI 注入、LLVM 烘焙（IR 常量解码验证）、自举终验（仓根直跑 70/0）；全部登记前现跑重筛，FRESH。；2026-09-03 批（内联 Pini 源码抽离收口）：新增 E-099..E-101——E-099 规模实测（三引号块 23 = PINI 15 + TOML 8，推翻工单原记 819；单行串含换行者 6–40B 均为断言期望值，抽离无收益）、E-100 抽离落地与语义等价（15 fixture，目标 22/22、全量 1144/0）、E-101 H-4 已落地致『解锁 H-4 签名迁移』动因失效（工单末条耦合风险随之过期）。**本次仅新增并现跑重筛 E-099..E-101；存量条目未重新验证，按其自身 validated_at 判定。**；2026-09-03T00:50+08:00 批（证据表清理工单立项）：新增 E-102（表内现状实测：100 条 / 97 条 status 失真 / 65 条 >72h / 61 条零引用 / E-008 悬空）。**本次仅新增并现跑重筛 E-102；存量条目未重新验证，按其自身 validated_at 判定。**；2026-09-03T01:37+08:00 批（证据表自动清扫落地）：新增 E-103——清扫机制挂载提交节拍、首轮 mark-only 实测 65/101、声明态漂移 100 → 0、删除路径经 /tmp 副本实跑验证 101 → 36。**本次仅新增并现跑重筛 E-103；存量条目未重新验证，按其自身 validated_at 判定。**；2026-09-04T09:28+08:00 批（G52 工单收口 + 批 7 远程 tap）：新增 E-104..E-106——E-104 批 7 落地实测（TapFetcher + 经典 MVS + resources 落地 + commit 真值；全量 1157/112/0，离线全链路），E-105 G52 工单「未完成」为文本未跟上的实测取证与归档代价，E-106 三项未实施条目的全仓核实（entry / graphOrder / resources 根检）。**本次仅新增并现跑重筛 E-104..E-106；存量条目未重新验证，按其自身 validated_at 判定。**；2026-09-04T09:35+08:00 批（G52 收尾补修）：新增 E-107——Def-1 resources 根检与 Def-7 锁文件 tap/source 回读，各带 1 个用例，全量 1159/112/0。**本次仅新增并现跑重筛 E-107；存量条目未重新验证，按其自身 validated_at 判定。**；2026-09-04T10:05+08:00 批（G52 第二批补修）：新增 E-108——**本次仅新增并现跑重筛 E-108；存量条目未重新验证，按其自身 validated_at 判定。**；2026-09-04T11:20+08:00 批 9（G52 全量收口）：新增 E-109——**本次仅新增并现跑重筛 E-109；存量条目未重新验证，按其自身 validated_at 判定。**"

[[evidence]]
id = "E-067"
assertion = "普通括号（最内层未闭合括号非块携带）内 NEWLINE 等同空白、缩进不参与：行尾不发 newline 记号、行首旁路缩进栈（空白照消费）"
spec_ref = "§2.2 括号内跨行 / §3 G55"
code_ref = "Sources/PiniCore/Lexer/Lexer.swift:21 bracketStack；:102 suppressesLayout"
validated_at = "2026-09-02T01:15:00+08:00"
status = "STALE"
note = "实测：bracketStack 项含 blockCarrying 标志；suppressesLayout = 栈顶为普通括号；行首分支按此旁路 IndentTracker"

[[evidence]]
id = "E-068"
assertion = "块携带括号判定：开括号后同行（仅跳空格/制表符）紧跟关键字 `func` 且后随非 IDENT 续字符；反引号转义不算，前瞻不跨行"
spec_ref = "§2.2 块携带括号 / §3 G55"
code_ref = "Sources/PiniCore/Lexer/Lexer.swift:129 isBlockCarryingOpen"
validated_at = "2026-09-02T01:15:00+08:00"
status = "STALE"
note = "实测：先跳空格/制表，再比 f/u/n/c，末位边界用 isLetter||isNumber||_ 排除；`func` 因首字符为反引号不匹配"

[[evidence]]
id = "E-069"
assertion = "自举 lexer 镜像同一抑制逻辑（字符串括号栈惯用法，pop 复用 st_pop）"
spec_ref = "§3 G55 / 自举探针"
code_ref = "examples/selfhost/src/lexer/lexer.pini:751 bst_push；:755 bst_top；:814 行首旁路；:839 行尾 newline 条件化"
validated_at = "2026-09-02T01:15:00+08:00"
status = "STALE"
note = "实测：四处落点与宿主一一对应；差分门禁 L0 MATCH 508"

[[evidence]]
id = "E-070"
assertion = "下标读（安全断言通道）返回元素本身，越界抛 RuntimeError.indexOutOfRange（诊断码 E5-005）；负索引尾部计数保留"
spec_ref = "§2.4.1 G48 / §3 G56 / ADR-028"
code_ref = "Sources/PiniCore/Interpreter/SubscriptStrategies.swift:91 SubscriptReadStrategy.read；Sources/PiniCore/Interpreter/RuntimeError.swift:31 case indexOutOfRange；Sources/PiniCore/Resources/Diagnostics.en.toml:126 [E5-005]"
validated_at = "2026-09-02T01:15:00+08:00"
status = "STALE"
note = "实测：越界程序报 Error: Runtime Error [E5-005]；E5-005 此前已在册且未被占用"

[[evidence]]
id = "E-071"
assertion = "下标读的类型推断由 Optional<T> 改为元素类型 T"
spec_ref = "§3 G56 / ADR-028"
code_ref = "Sources/PiniCore/Type/TypeInference.swift:230 infer 的 .subscript 分支"
validated_at = "2026-09-02T01:15:00+08:00"
status = "STALE"
note = "实测：Array→params.first、Dictionary→params.last、String→String；不再包 Optional"

[[evidence]]
id = "E-072"
assertion = ".get(i) / .getUnchecked(i) 登记为内建成员方法（String/Array/Dictionary，inTrait=false）"
spec_ref = "§3 G56 / ADR-028"
code_ref = "Sources/PiniCore/Common/BuiltinRegistry.swift:198 get(String)；:213 get(Array)；:219 get(Dictionary)"
validated_at = "2026-09-02T01:15:00+08:00"
status = "STALE"
note = "实测：三类型各有 get 与 getUnchecked 两条；inTrait=false 故不进 collection 特征面"

[[evidence]]
id = "E-073"
assertion = "解释器单一分派点处理三类型；UB 以「未定义行为陷阱」近似"
spec_ref = "§3 G56 / ADR-028"
code_ref = "Sources/PiniCore/Interpreter/Interpreter.swift:2650 uncheckedOrNone；callFunctionValue 内 get/getUnchecked 分支"
validated_at = "2026-09-02T01:15:00+08:00"
status = "STALE"
note = "实测：get 越界→.enumValue(none)；getUnchecked 越界→抛 E5-006 并在消息中说明为 UB 近似"

[[evidence]]
id = "E-074"
assertion = "字典 .get(key) 的键按任意值匹配（非整数索引）"
spec_ref = "§3 G56 / ADR-028"
code_ref = "Sources/PiniCore/Interpreter/Interpreter.swift:2980 字典分支先于索引校验分流"
validated_at = "2026-09-02T01:15:00+08:00"
status = "STALE"
note = "实测：d.get(`x`) 正常；批 2 初版曾误要求整数索引，本轮修复"

[[evidence]]
id = "E-075"
assertion = "字典成员方法派发路径（原无，为 .get 新增）"
spec_ref = "§3 G56 / ADR-028"
code_ref = "Sources/PiniCore/Interpreter/Interpreter.swift:2207 evaluateMember 的 .dictionary 分支"
validated_at = "2026-09-02T01:15:00+08:00"
status = "STALE"
note = "实测：d.get(k) / unsafe d.getUnchecked(k) 可解析并运行"

[[evidence]]
id = "E-076"
assertion = "LLVM 运行时下标读越界 bk_panic（本批据此定性为「向 LLVM 收敛」）"
spec_ref = "§3 G56 / ADR-028"
code_ref = "Sources/PiniRuntime/PiniRuntime.swift:239 bk_array_get（OOB → bk_panic）"
validated_at = "2026-09-02T01:15:00+08:00"
status = "STALE"
note = "实测（源码级；本环境无 lli，未实跑，按 D9 如实标注）"

[[evidence]]
id = "E-077"
assertion = "调用实参与泛型构造实参的标签用 `=`；旧 `:` 记法报错并给迁移提示"
spec_ref = "§2.2 记法原则 / §3 G57 / ADR-029"
code_ref = "Sources/PiniCore/Parser/Parser.swift:2875（泛型构造）、:3071（调用）"
validated_at = "2026-09-02T01:15:00+08:00"
status = "STALE"
note = "实测：identifier+assign 判为标签；identifier+colon 抛「实参标签须用 = …已废弃」"

[[evidence]]
id = "E-078"
assertion = "标签元组元素用 `=`"
spec_ref = "§2.2 / §3 G57 / ADR-029"
code_ref = "Sources/PiniCore/Parser/Parser.swift:2911 parseTupleOrParen 内标签元素判定（:2938 迁移提示）"
validated_at = "2026-09-02T01:15:00+08:00"
status = "STALE"
note = "实测：`(x = 1, y = 2,)` 可解析；(a: 1,) 抛「元组标签须用 =」"

[[evidence]]
id = "E-079"
assertion = "字典字面量条目用 `=`（靠首个元素后是否 assign 消歧数组/字典）"
spec_ref = "§2.2 / §3 G57 / ADR-029"
code_ref = "Sources/PiniCore/Parser/Parser.swift:2977 parseCollectionLiteral（:2990 首条目提示、:3004 后续条目校验）"
validated_at = "2026-09-02T01:15:00+08:00"
status = "STALE"
note = "实测：`[`Alice` = 30]` 成立；`[`a`: 1]` 抛「字典条目须用 =（`[:]` 为全切片）」"

[[evidence]]
id = "E-080"
assertion = "match 具名绑定保留 `:`（取出方向，D-5）"
spec_ref = "§3 G57 / ADR-029 / G54"
code_ref = "Sources/PiniCore/Parser/Parser.swift:2348 colon 判定（ADR 具名关联值决议注释在案）"
validated_at = "2026-09-02T01:15:00+08:00"
status = "STALE"
note = "实测：`case 值(v: x):` 正常；该分支未被本轮改动"

[[evidence]]
id = "E-081"
assertion = "自举 parser 同步 `=` 标签（parse_element 与实参表）"
spec_ref = "§3 G57 / 自举探针"
code_ref = "examples/selfhost/src/parser/parser.pini parse_element 与实参表（assign 判定）"
validated_at = "2026-09-02T01:15:00+08:00"
status = "STALE"
note = "实测：同步后 parse 差分重新 MATCH 222 + 94"

[[evidence]]
id = "E-082"
assertion = "缩进函数体内空行透明（不闭块，语句跨空行连续执行）"
spec_ref = "§2.3 块文法 / F5"
code_ref = "探针实测：main 体 print(1) 与 print(2) 以空行分隔，输出 1、2"
validated_at = "2026-09-02T01:40:00+08:00"
status = "STALE"
note = "2026-09-02 01:20 探针；与自举 lexer『空行全透明、缩进栈不动』一致"

[[evidence]]
id = "E-083"
assertion = "顶格与缩进的字段块内空行透明（字段跨空行存活）"
spec_ref = "§2.3 块文法 / F5"
code_ref = "探针实测：`(P)` 体字段 x、y 以空行分隔，P() 后 p.x、p.y 均存在"
validated_at = "2026-09-02T01:40:00+08:00"
status = "STALE"
note = "2026-09-02 01:20 探针；**推翻 F5 提议『空行闭块』假设**（S5/S6 时代产出，从未验证）"

[[evidence]]
id = "E-084"
assertion = "顶格字段块闭合于下一顶级声明起点（无空行隔断亦正确闭合）"
spec_ref = "§2.3 块文法 / F5"
code_ref = "探针实测：字段块后紧随 `main|func`（无空行），main 正确解析且字段完整"
validated_at = "2026-09-02T01:40:00+08:00"
status = "STALE"
note = "2026-09-02 01:20 探针"

[[evidence]]
id = "E-085"
assertion = "字段块内裸语句被拒（字段块只接受字段行）"
spec_ref = "§2.3 块文法 / F5"
code_ref = "探针实测：字段后随 `print(1)` → Parse Error E2-001"
validated_at = "2026-09-02T01:40:00+08:00"
status = "STALE"
note = "2026-09-02 01:20 探针"

[[evidence]]
id = "E-086"
assertion = "空行在记号层结构上不可见：两套 lexer 均不产记号、解析器无线号差逻辑；字段块内含空行的差分实验 MATCH"
spec_ref = "§2.3 块文法 / F5 / 门禁盲区记录"
code_ref = "Sources/PiniCore/Lexer/Lexer.swift:203 skipBlankOrCommentLine；examples/selfhost/src/lexer/lexer.pini blank-line 分支；语料探针段（parse_slice_corpus.pini 尾部 PP 块）"
validated_at = "2026-09-02T01:40:00+08:00"
status = "STALE"
note = "差分：字段块内含空行，宿主与自举 parse MATCH 253——门禁此前盲区（语料空行恰都后随块头）已由该探针段永久覆盖"

[[evidence]]
id = "E-087"
assertion = "IO 相对路径按形态三段式解析：绝对原样；./ ../ 相对运行时 CWD；其余相对程序基准（模块=模块根 / 单文件=入口目录）"
spec_ref = "§2.4.1 G14/G58 / ADR-030"
code_ref = "Sources/PiniCore/Interpreter/Interpreter.swift:147 programBase；:160 resolveIOPath；Sources/PiniCLI/main.swift:679 absoluteProgramBase"
validated_at = "2026-09-02T18:32:00+08:00"
status = "STALE"
note = "实测 18:30：从 /tmp 跑 /tmp/base_mod，readFile(res.txt) 读到模块根文件、./res.txt 按 CWD 找不到 panic（设计语义）"

[[evidence]]
id = "E-088"
assertion = "moduleRoot() 内建返回程序基准；CLI 六执行入口注入基准；子解释器继承父基准"
spec_ref = "§3 G58 / ADR-030"
code_ref = "Sources/PiniCore/Common/BuiltinRegistry.swift:111；Sources/PiniCore/Interpreter/Interpreter.swift loadImports 子解释器传参；main.swift run/调试器/测试六入口"
validated_at = "2026-09-02T18:32:00+08:00"
status = "STALE"
note = "实测 18:30：moduleRoot() 从 /tmp 输出 /tmp/base_mod；未注入基准时如实返回 CWD"

[[evidence]]
id = "E-089"
assertion = "LLVM 端无前缀相对路径字面量编译期烘焙基准；./ ../ 与绝对路径原样；非字面量路径不烘焙（v1 限制）；moduleRoot 在 LLVM 端显式 unsupported"
spec_ref = "§3 G58 / ADR-030 / D-4"
code_ref = "Sources/PiniCore/CodeGen/IRGenerator.swift:107 programBase；Sources/PiniCore/CodeGen/Emitters/BuiltinsEmitter.swift:173 generateIOPathArgument；Sources/PiniCore/CodeGen/Emitters/ExprEmitter.swift:663 unsupported"
validated_at = "2026-09-02T18:32:00+08:00"
status = "STALE"
note = "实测 18:35：emit IR 常量解码为 /tmp/base_mod/res.txt + NUL（22 字节），fopen 实参经 @.str0 GEP 引用"

[[evidence]]
id = "E-090"
assertion = "自举测试从仓根直跑全绿（原 5 个 CWD 依赖假失败零改动修复）——IO 相对路径按模块根解析的终验"
spec_ref = "§3 G58 / ADR-030 / 终验"
code_ref = "探针实测：pini test examples/selfhost 从仓根直跑 70 通过 0 失败；IOTests（绝对路径注入兼容面）6/0"
validated_at = "2026-09-02T18:32:00+08:00"
status = "STALE"
note = "实测 18:37；测试文件零改动——语料中 readFile(examples/...) 相对模块根书写，新基准下语义自然正确"

[[evidence]]
id = "E-091"
assertion = "清单双通道解析：tap/require/resources/replace 及点分子表；[[ ]] 数组表；旧 [dependencies] 命中即报错并指引（D-B）"
spec_ref = "§2.5 G52 / 批 6"
code_ref = "Sources/PiniCore/Common/MiniTOML.swift:8 MiniTOML.parse；Sources/PiniCore/Common/FileLoader.swift parseManifest（legacyDependenciesSection）"
validated_at = "2026-09-02T20:05:00+08:00"
status = "STALE"
note = "实测：四节+子表+replace 全解析；[dependencies] 报错含 [require]/[resources]/tidy 三指引；测试 ModuleToolchainTests 3 用例"

[[evidence]]
id = "E-092"
assertion = "SHA-256 纯 Swift 实现（FIPS 180-4）过 NIST 向量；treeSum 字典序遍历、篡改一字节必变、排除 .git/.build/锁文件自身、.pini/ 不排除"
spec_ref = "§2.5 G52 校验和 / D6"
code_ref = "Sources/PiniCore/Common/SHA256.swift:7 SHA256；Sources/PiniCore/Module/ModuleToolchain.swift treeSum"
validated_at = "2026-09-02T20:05:00+08:00"
status = "STALE"
note = "实测：空串/abc/百万 a 三向量 + 篡改敏感与排除行为两断言（ModuleToolchainTests）"

[[evidence]]
id = "E-093"
assertion = "MVS v1（每依赖单可用版本、全部 requirer 约束须满足，否则列出各方要求）+ 锁文件生成/回读（G52 §3.5 格式，imported-by/graph.order）"
spec_ref = "§2.5 G52 R3 / D16"
code_ref = "Sources/PiniCore/Module/ModuleToolchain.swift resolve / renderSummary / parseSummary"
validated_at = "2026-09-02T20:05:00+08:00"
status = "STALE"
note = "实测：三模块夹具多 requirer imported-by 与拓扑序正确；冲突报错列各方要求；锁文件回读一致"

[[evidence]]
id = "E-094"
assertion = "四命令 tidy/refresh/verify/graph 全量落地 + build 漂移检查（采用门控）+ 批 1 缺口修复（run(package:) 补 loadImports）"
spec_ref = "§2.5 G52 §4 命令 / 批 6 阶段 4-5"
code_ref = "Sources/PiniCLI/main.swift:682 runModCommand、:1501 case `mod`；Sources/PiniCore/Module/ModuleToolchain.swift:502 tidy、:585 refresh、:602 verify、:635 graph、:704 checkRequireImportAlignment；Sources/PiniCore/Interpreter/Interpreter.swift:319 loadImports"
validated_at = "2026-09-02T20:05:00+08:00"
status = "STALE"
note = "实测（/tmp 夹具 8 步）：漂移报错→tidy 新增→check 绿→refresh 30 行→verify 2/2→graph 缩进树→篡改报 2 处→run 跨模块调用输出「来自 uni」；全量 1135/0"

[[evidence]]
id = "E-095"
assertion = "注入导入语义：`_别名 = path` 后目标 public 符号可裸调用与 `_别名.符号` 限定；非 `_` 别名裸调用被语义层拒绝（E3-002）；三类冲突均 E3-013"
spec_ref = "§2.5 D-4 / issue-module-batch6"
code_ref = "Sources/PiniCore/Semantic/SemanticAnalyzer.swift setupInjections + requireDefined 注入放行；Sources/PiniCore/Interpreter/Interpreter.swift resolveInjectedBare；SymbolTable forbiddenNames 哨兵"
validated_at = "2026-09-02T21:30:00+08:00"
status = "STALE"
note = "实测（/tmp/d4demo 六场景 + ImportInjectionTests 4/0）：裸调用与限定双输出；三类冲突报错点名双方与修复建议"

[[evidence]]
id = "E-096"
assertion = "D-4 前置缺口补齐：多项 import 块（原批 1 单项限制解除，pending 缓冲保序）；R1 父扫描嵌套清单排除（G52 ① 词法包含，此前仅 import 加载侧实现）"
spec_ref = "§2.5 G52 ① / issue-module-batch1 D-4 前置"
code_ref = "Sources/PiniCore/Parser/Parser.swift parseImportBlock + pendingImportDecls；Sources/PiniCore/Common/FileLoader.swift loadDirectory 嵌套排除"
validated_at = "2026-09-02T21:30:00+08:00"
status = "STALE"
note = "实测：两注入模块同名导出触发 E3-013（多项 import 必需）；全量 1139/0 无回归"

[[evidence]]
id = "E-097"
assertion = "Def-1/2/3 消化：子进程 CLI 测试基建；注入跨模块传递；单文件 run 语义门禁 + 警告输出"
spec_ref = "issue-d4-deferred-defects（已归档 archive/） / §2.5 D-4"
code_ref = "Sources/PiniCore/Interpreter/Interpreter.swift loadImports 合并段；Sources/PiniCLI/main.swift runRunPath 语义门禁；Tests/PiniTests/ImportInjectionTests runCLI harness"
validated_at = "2026-09-02T21:50:00+08:00"
status = "STALE"
note = "实测：uni 内部注入裸调 greet 经 uni.hello() 输出「来自注入」；坏名单文件 run 报 E3-002；同包双 main 被拒"

[[evidence]]
id = "E-098"
assertion = "F6：argv() 内建返回脚本路径之后裸参数（单文件/模块双形态；空参空数组；LLVM unsupported）"
spec_ref = "§2.4.1 io 行 / F6"
code_ref = "Sources/PiniCore/Common/BuiltinRegistry.swift argv 登记；Sources/PiniCLI/main.swift runRunPath(argv:)；Sources/PiniCore/CodeGen/Emitters/ExprEmitter.swift argv unsupported"
validated_at = "2026-09-02T21:50:00+08:00"
status = "STALE"
note = "实测：[甲, 乙] / [] / [X] 三形态输出；测试 testArgvPassthroughViaCLI"

[[evidence]]
id = "E-099"
assertion = "内联 Pini 源码的真实规模为 15 块三引号源码（ParenEqualsTests 11 + IOTests 4），而非工单原记的 819；另有无抽离价值的 TOML 配置块 8 个（非 Pini 源码）"
spec_ref = "docs/issue-inline-pini-extraction-2026-08-31.md 现状表（已据此修正）"
code_ref = "Tests/PiniTests/ 全量扫描：三引号块 23 = PINI 15 + TOML 8（ModuleTestCollectionTests 3 / ModuleToolchainTests 5）；PINI 分布于 ParenEqualsTests 11、IOTests 4"
validated_at = "2026-09-03T00:13:09+08:00"
status = "STALE"
note = "实测：grep -ro 统计 Tests/PiniTests 下三引号共 46 次 = 23 对块；按 Pini 关键字（|func/main|/->/var/return…）与 TOML 关键字（[package/name =/version/dependencies…）打分分类。另扫得喂给 Pini 前端的单行串 290 个，其中含换行的字节数仅 6–40B（如 RuntimeBackendTests 的 7B、StdlibTests 的 6B），均为 XCTAssertEqual 的期望输出而非源码——抽离是负收益，已裁决不抽"

[[evidence]]
id = "E-100"
assertion = "15 块内联 Pini 源码已抽离为 <测试函数名>.pini 夹具，经 loadPiniFixture(_:filePath:) 加载；Swift 多行字符串缩进剥离后与文件内容一致，测试语义等价"
spec_ref = "docs/issue-inline-pini-extraction-2026-08-31.md 命名与路径规则 / 保真校验"
code_ref = "Tests/PiniTests/ParenEqualsTests/*.pini（11）+ Tests/PiniTests/IOTests/*.pini（4）；Tests/PiniTests/PiniFixtureLoader.swift loadPiniFixture；IOTests testAbsolutePathUnaffectedByBase 用 __PATH__ 占位符"
validated_at = "2026-09-03T00:13:09+08:00"
status = "STALE"
note = "实测：目标测试 22/22（ParenEqualsTests 11 + IOTests 11）通过，全量 1144 tests / 0 failures / 20 skipped，与抽离前基线一致。剥离规则 = 去掉结束定界符缩进量并保留相对缩进，产物不以换行结尾（与既有 IOTests/testReadFile.pini 一致）。含 Swift 插值 1 块沿用既有 __PATH__ 占位符模式（file → .replacingOccurrences(of:_:with:)）。3 个原非 throws 测试函数补 throws 而非 try!（加载失败应使测试失败）"

[[evidence]]
id = "E-101"
assertion = "H-4（带执行块的函数一律带冒号）已于 2026-08-31 阶段 2 落地，抽离工单中『解锁 H-4 的 862 处签名迁移』这一附带收益动因失效"
spec_ref = "docs/spec/issue/issue-conflict-register-2026-08-31.md 收敛项 H-4"
code_ref = "docs/spec/issue/issue-conflict-register-2026-08-31.md H-4 条目（状态：完成；2026-08-31 阶段 2，宿主三路径转强制 + 存量补冒号 1089 行/804 文件 + spec A.2.3 钉定 + 自举同步）"
validated_at = "2026-09-03T00:13:09+08:00"
status = "STALE"
note = "H-4 在抽离仅完成 0.6% 时即绕道落地，未依赖抽离。故抽离工单末条风险『与 H-4 的耦合：抽离完成后再执行冒号强制迁移，顺序不可颠倒』已过期；同批修正。抽离剩余动因回到首条：内联字符串对换行/缩进/转义的处理不受 Pini 自身约束"

[[evidence]]
id = "E-102"
assertion = "证据表 2026-09-03 现状实测：dict 载体 100 条，其中 97 条已越过 §1.4 的 60 分钟窗口却仍标 FRESH——status 字段零执行，失真率 97%；docs 下 13 个 E-ID / 18 处引用，1 处悬空（E-008）"
spec_ref = "pini-spec-v0.md §1.4 证据新鲜度协议（60 分钟粒度 / FRESH-STALE 二态）"
code_ref = "docs/spec/evidence-table.toml 经 tomllib.load() 后 len(doc['evidence'])；docs/**/*.md 正则 \\bE-?\\d3\\b 扫描"
validated_at = "2026-09-03T00:50+08:00"
status = "STALE"
note = "实测（tomllib 解析，非文本计数）：总条数 100；年龄分桶 ≤60min=3、1h-72h=32、>72h=65；status 全为 FRESH（100/100），最早 validated_at=2026-08-25T00:15。引用面：docs 下引用 13 个不同 E-ID 共 18 处，其中 12 个在表内、E-008 不在（悬空，2 处引用，位于 pini-roadmap-next.md 等）；被引用最多的 E-047×3。删除面：65 个 >72h 候选中仅 E-035(×1) 与 E-047(×3) 被引用，61 条零引用。此证据为工单 issue-evidence-table-sweep-2026-09-03.md 的事实基础"

[[evidence]]
id = "E-103"
assertion = "证据表自动清扫已挂到提交节拍：101 条中 65 条越过 72h 被标 PENDING_DELETE；删除阶段最早 2026-09-04T01:32 触发（24h 节流）"
spec_ref = "pini-spec-v0.md §1.4 自动清扫；工单 docs/spec/issue/issue-evidence-table-sweep-2026-09-03.md"
code_ref = "tools/evidence_sweep.py（--stats / --check / --apply / --hook）；hooks/pre-commit 末段清扫块"
validated_at = "2026-09-03T01:37+08:00"
status = "STALE"
note = "实测（现跑，非文本计数）：tomllib 解析得 101 条；计算态 FRESH 1 / STALE 35 / PENDING_DELETE 65；声明态漂移 100 条，首轮 --apply 后归 0（status 字段此前从未被执行，其记录的是『写入时有人声称』而非『现在可信』）。首轮为 mark-only：diff 校验仅 200 行 status + 2 行 meta（last_sweep、dangling_ids），assertion / code_ref / note 零改动。删除路径用 /tmp 副本 + --force 实跑：101 → 36，并报告 E-035×1（docs/issue-unicode-char-predicates-2026-08-29.md:41）与 E-047×3（docs/spec/pini-landing-plan-v048.md:45/46/87）将悬空、记入 meta.dangling_ids；仓内表未受影响。节流路径（swept 0:04:00 ago）与触发路径（副本 101 → 36）均已实跑。当前全仓悬空引用为 0：E-008 的两条废弃指令（pini-landing-plan-v048.md 旧 46/87 行）已删，工单自身对 ID 的讨论经 meta.scan_exclude 排除于扫描之外。教训（三轮内自犯三次）：在提交文档里写裸的未分配编号会自注册成伪引用，写作时用 E-NNN 占位符表述格式。"

[[evidence]]
id = "E-104"
assertion = "批 7 已落地：github: / git: 远程 tap 由 TapFetcher 经 git 子进程抓取；MVS 为经典形态（tag 候选取满足全部约束的最小版本）；resources 由 refresh 落地到 .pini/resources/<name>/；锁文件 commit 成为真实来源定位符（不再恒为 -）"
spec_ref = "docs/spec/issue/archive/issue-module-system-rules-2026-08-28.md 第 7 节验收记录；docs/spec/pini-spec-v0.md §2.5 版本与命令"
code_ref = "Sources/PiniCore/Module/TapFetcher.swift（expand / availableVersions / selectVersion / materialize / headCommit / runGit）；Sources/PiniCore/Module/ModuleToolchain.swift（FetchSession / ensureMaterialized / reconcileSelection / resolveFetching / refresh）；Tests/PiniTests/ModuleToolchainTests/TapFetcherTests.swift"
validated_at = "2026-09-04T09:28+08:00"
status = "FRESH"
note = "实测：swift test --disable-sandbox --scratch-path /tmp/pini-build 全量 1157 执行 / 112 跳过 / 0 失败（批 7 前基线 1144，净增 13）。关键设计：git: 协议接受本地路径，故用 tmp 里的真实 git 仓库当远端，ls-remote 到 MVS 到 clone 到 checkout 到 rev-parse 全链路离线跑通，不发网络包。新增用例：refresh 端到端（落地 1.2.0 而非 2.0.0、commit 为 40 位 SHA-1、verify 通过、篡改后检出不符）、resources 落地、传递约束收敛（a 的 ^1.2 压过 root 的 * 回填，收敛到 1.2.0）。既有 testResolveRemoteTapErrorsOutPerDA 已改判为两例——批 6 的 D-A「远程一律报错」收窄为「未落地才报错」。本环境构建必须加 --scratch-path，否则 SwiftPM 卷内 .TemporaryItems 被沙箱拒（换 TMPDIR 无效）"

[[evidence]]
id = "E-105"
assertion = "G52 工单的「未完成」是文本未跟上而非实现缺失：决议 R1–R8 / D1–D28、三处规范登记与宿主实现（批 1 / 批 6）均已交付；2026-09-04 收口并归档，遗留 8 项缺陷登记于其第 9 节"
spec_ref = "docs/spec/issue/archive/issue-module-system-rules-2026-08-28.md 第 6 节落点 / 第 7 节验收记录 / 第 9 节遗留"
code_ref = "docs/spec/pini-spec-v0.md:281（版本与命令）与 :367（G52 决策行）；docs/spec/pini-project-spec.md 第 3/4 节（.gitignore 含 !pini-summary.toml）与第 7 节（tap / require / resources / replace，dependencies 标 Removed、§7.3 版本约束语法）；docs/spec/pini-roadmap-next.md T2 行与 §8.1 批次登记表"
validated_at = "2026-09-04T09:28+08:00"
status = "FRESH"
note = "实测（逐项到代码与规范里找落点，不数复选框）：9 条 DoD 中 6 条通过、1 条部分、2 条未实现、1 条判据过时（deps/pini-swift 归类为 resources 因 ADR-024 D2 已失效）。治理教训：同一次会话里两次因 BSD grep 在 BRE 下不支持竖线分隔、静默返回空，误判「spec v0 未登记 G52」与「project-spec 第 7 节未更新 schema」——两者其实都已落地；判定「某项未做」前须先自测查询本身。归档代价：7 处入向引用全改（含 1 处裸文件名），工单自身 6 处相对链接因多一层目录改写为仓库相对路径；改完 check-doc-links 通过（240 引用）"

[[evidence]]
id = "E-106"
assertion = "G52 三项实施条目经全仓搜索确认未实现：[[bin]]/[lib] entry 零消费点；graph.order 只写不读（解释器未按拓扑序注册模块）；resources 根检（R7 反向）未拦，双通道只兑现正向"
spec_ref = "docs/spec/issue/archive/issue-module-system-rules-2026-08-28.md 第 9 节 Def-1 / Def-2 / Def-3"
code_ref = "Sources/ 全仓搜 entry（词边界）除注释外零命中；Sources/ 全仓搜 graphOrder 仅 ModuleToolchain.swift 的锁文件读写（定义 / 生成 / 渲染 / 解析），无解释器消费点；ModuleToolchain.swift 的 resources 循环无「目标根含 pini.toml 即拒」判定"
validated_at = "2026-09-04T09:28+08:00"
status = "FRESH"
note = "全仓核实：entry 除注释外零出现，入口仍为「全局找 main」，故 G49 的双 main 冲突（E3-004）未根治。graphOrder 只在 renderSummary / parseSummary 出现，解释器不消费，锁文件 order 字段形同虚设。双通道仅兑现正向（import 目标不是模块 → ToolchainFailure.importTargetNotModule），反向未实现，DoD 第 5 条只过一半。三者均登记为第 9 节缺陷，按项目工作法不在本批修复"

[[evidence]]
id = "E-107"
assertion = "G52 收尾补修两项：Def-1（resources 根检，双通道至此双向封闭）与 Def-7（锁文件 tap / source 回读，verify 报错回显来源）"
spec_ref = "docs/spec/issue/archive/issue-module-system-rules-2026-08-28.md 第 9 节 Def-1 / Def-7；docs/spec/pini-spec-v0.md §2.5 与 G52 决策行"
code_ref = "Sources/PiniCore/Module/ModuleToolchain.swift：ToolchainError.resourceTargetIsModule（resources 循环的根检）；Summary.modules / Summary.resources 元组增 tap 与 source，parseSummary 回读；verify 的 originSuffix 回显来源；Tests/.../TapFetcherTests.swift testResourceTargetThatIsAModuleIsRejected / testSummaryRoundTripsTapSourceAndVerifyEchoesOrigin"
validated_at = "2026-09-04T09:35+08:00"
status = "FRESH"
note = "实测：全量 1159 执行 / 112 跳过 / 0 失败（补修前 1157，+2）。Def-1 判据 = 资源目标根含 pini.toml 即抛错并指引改用 [require]，只查根不查深层（R7 明定）。Def-7 的动机：此前 renderSummary 写 tap/source/commit 而 parseSummary 全丢弃——commit 恒为 - 时无人察觉，批 7 使其成真值后暴露；回读后 verify 报错形如「模块 X 内容与锁文件不符（deps/X）；来源 <url>」，篡改时才知道该去哪里核对。两项均带 1 个用例。剩余 Def-2/3/4/5/6/8 维持未修，按项目工作法由后续批次承接"

[[evidence]]
id = "E-108"
assertion = "G52 第二批补修：Def-6（`[replace]` 三种形态全部生效 + `file:` 形态不再往用户本地目录抓取）与 Def-5（三层嵌套模块用例）"
spec_ref = "docs/spec/issue/archive/issue-module-system-rules-2026-08-28.md 第 3.7 节（D13）/ 第 9 节 Def-5 / Def-6 / Def-9..Def-12；docs/spec/pini-spec-v0.md §2.5 版本与命令、G52 决策行"
code_ref = "Sources/PiniCore/Module/ModuleToolchain.swift：enum Replacement（version / local / fork）、parseReplace、stripVersionPrefix、replaceTapName、resolve 内的 replacements 预注入约束与 spec/tap 改写；Tests/PiniTests/ModuleToolchainTests/TapFetcherTests.swift testParseReplaceClassifiesThreeForms / testReplaceVersionOverrideRaisesSelection / testReplaceVersionOverrideConflictSurfacesReplaceConstraint / testReplaceForkRedirectsSourceAndRecordsReplaceTap / testReplaceForkPinParticipatesInMVS / testReplaceFileFormDoesNotFetchIntoLocalDirectory / testDependencyReplaceIsIgnored；Tests/PiniTests/ModuleSystemTests/ModuleSystemTests.swift testThreeLevelNestedModulesStaySeparate + demo3/ 夹具"
validated_at = "2026-09-04T10:05+08:00"
status = "FRESH"
note = "实测：全量 1167 执行 / 112 跳过 / 0 失败（补修前 1159，+8：Def-6 的 7 个 + Def-5 的 1 个）。Def-6 判据：D13 三种形态此前只有 file: 生效（resolve 里 `rep.hasPrefix(\"file:\")` 单点判断），版本覆盖与 fork 静默落空；现由 parseReplace 归类，版本类（覆盖与 fork 的 @pin）以 requirer `<replace>` 预注入约束账本——必须在遍历前注入，否则 ensureMaterialized 首抓看不到、只能靠事后复核兜底（白抓一次 + 多走一轮迭代）。fork / local 形态锁文件 tap 记 replace。Def-5 判据：三层 app ⊃ frontend ⊃ syntax，frontend 与 syntax 各自定义同名 public 符号 取值（100 / 10），输出 110；lex.pini 距其清单两层，是唯一能验证 loadDirectory 祖先回溯递归性的位置。附带发现并新登记 Def-9（versionComponents(\"v1.0\") → [0]）/ Def-10（file: 替换 + 远程 tap 会往用户本地目录 fetch+checkout，批 7 回归，本次随 Def-6 一并修）/ Def-11（loadDirectory 递归扫子树 vs ModuleDependencyLoader 只扫模块根级）/ Def-12（锁文件 tap 取最后遍历到的 requirer 边，多 requirer 时无确定规则）——均未修，仅登记"

[[evidence]]
id = "E-109"
assertion = "G52 批 9 全量收口：§9 全部 12 项缺陷闭环——Def-3（[[bin]].entry 入口定位）/ Def-2（graph.order 导出视图 + 三层链钉子）/ Def-9（v 前缀）/ Def-12（锁文件取边规则）/ Def-11（扫描策略按来源划分）/ Def-8（资源寻址 v1 不提供）；Def-4 在自举仓独立提交"
spec_ref = "docs/spec/issue/archive/issue-module-system-rules-2026-08-28.md 状态块（五批次表）/ 第 9 节（全表改写）；docs/spec/pini-spec-v0.md G52 决策行（批 9 收口句）；docs/spec/pini-project-spec.md §3.3 资源寻址注记；docs/spec/pini-roadmap-next.md T2 与 §8.1 批 9 行"
code_ref = "Sources/PiniCore/Common/FileLoader.swift：ModuleManifest.entryPoints（[[bin]].entry / [lib].entry 解析去重）；Sources/PiniCore/Interpreter/Interpreter.swift：entryFiles / mainDeclaringFile / checkEntryConsistency / normalizeEntryPath（executeMain 与 runSuspendable 双路接入）、registerDecls 的 funcDecl main 记录；Sources/PiniCore/Interpreter/RuntimeError.swift：entryMainMismatch（runtime-018）；Sources/PiniCLI/main.swift：目录 run 注入 entryFiles；Sources/PiniCore/Module/ModuleToolchain.swift：Resolution.graphOrder 导出视图定性；Sources/PiniCore/Module/ModuleDependencyLoader.swift + FileLoader.parseUnit：扫描按 deps 父目录划分；Tests/PiniTests/CLIDirectoryTests/CLIDirectoryTests.swift testEntryDeclaredAndSatisfiedRunsNormally / testEntryInSubdirectorySrcLayout / testEntryMismatchThrowsDiagnosticError / testNoEntryKeepsGlobalMainLookup；Tests/PiniTests/ModuleSystemTests/ModuleSystemTests.swift testDependencyChainIsReadyBeforeDependentRuns；Tests/PiniTests/ModuleToolchainTests/ModuleToolchainTests.swift testSummarySourcePrefersLexicographicallyFirstRequirer"
validated_at = "2026-09-04T11:20+08:00"
status = "FRESH"
note = "实测：全量 1178 执行 / 112 跳过 / 0 失败（批 8 收口 1167，净增 11 = Def-9 的 3 + Def-12 的 1 + Def-11 的 2 + Def-3 的 4 + Def-2 的 1；1167+11 与逐批实测吻合）。Def-3 判据：entry 四态 CLI 实测——无声明跑 42 不变 / 声明满足跑 42 / 声明指错 other.pini 报 entryMainMismatch 且 rc=1 / src/ 布局子目录 entry 跑 7；变异实验（checkEntryConsistency 改 no-op）使 mismatch 测试转红。Def-12 判据：夹具经两轮迭代才具区分力——旧规则（每边覆写 = 最后遍历者胜）等价于「最浅 requirer 胜出」，与字典序规则只在浅层 requirer 名字典序更大时分岔，夹具取 z（浅）/a（深）分得 tz vs ta；回退验证测试真转红。Def-2 判据：纯注册顺序无可观测副作用，钉的是「依赖环境先于依赖者可用」——三层 app ⊃ mid ⊃ leaf 链，mid 函数体解析 leaf 导出符号、app main 调 mid，输出 7；graphOrder 注释定性为导出视图。Def-8：规范侧关闭（v1 不提供 + 判据：零真实用例，待第二个用例再定型），无代码变更。自举仓 Def-4：examples/pini.toml 使 R1 自切，摘掉 exclude 实测 check 仍 15 文件 ⇒ 两机制独立；pini test . 70 通过不变。E3-004 更正：双 main 经实测上游已诊断为 redeclared symbol，工单原文「未根治」表述有误，已随 Def-3 行改写更正"

[[evidence]]
id = "E-110"
assertion = "F5 缺陷实测复现并立案：安全上下文（main|func）内裸调 foreign 函数（ffi_malloc(16)）无 unsafe 消耗点，静态层与运行时均不拦截，返回裸指针；门禁位点定位——TypeChecker.checkExpression 中 .addressOf / .forceUnwrap 有 unsafeContextDepth 门禁、.unsafe 开上下文，foreign 调用分派位（.call → foreignDecl 符号）无对应门禁"
spec_ref = "docs/issue-unsafe-gate-foreign-2026-09-04.md（立案全文：复现探针 + 门禁位点表 + 修复方向）；docs/spec/pini-spec-v0.md §A.2.2（块内函数自动 |unsafe）与 §2.7；docs/spec/issue/archive/issue-draft-audit-disposition-2026-08-31.md §五 F5 行（改指新工单）"
code_ref = "Sources/PiniCore/Type/TypeChecker.swift：checkExpression 的 .addressOf / .unary(.forceUnwrap) 门禁（unsafeContextDepth）与 .unsafe 消耗点——foreign 调用位无门禁即本缺陷"
validated_at = "2026-09-04T17:40+08:00"
status = "FRESH"
note = "探针实测：/tmp/f5probe（[ffilib|foreign] 声明 ffi_malloc(size: U64,) -> (*U8,)，vendored libffilib.dylib + search_paths=[\"lib\"] + libs），main|func 内裸调 let p = ffi_malloc(16) → pini run . 正常输出 *U8@0x...，无 E4-001/无运行时拦截。与 ADR-028 D-4（空 unsafe 消耗点无害）正交：本缺陷是『该消耗而未消耗』反向缺口"

[[evidence]]
id = "E-111"
assertion = "ffi-module 工单 §二 G2 的『load 返回动态 Any，仅 unsafe load(p) 才能收回具体标量』为 ADR-015 落地前的旧观测，已过时——实测 main（非 |unsafe 函数体）内裸 load(p) 不报 E4-001，按指针元素类型编解码返回类型化值；examples/ffi_module/cstring.pini 头部同源注释同属过时陈述"
spec_ref = "docs/spec/pini-spec-v0.md §2.7（指针原语 load/store/addressof 按元素类型编解码行）；docs/issue-ffi-module-2026-08-27.md §二 G2（时间线注记落点）"
code_ref = "Sources/PiniCore/Interpreter/Interpreter.swift：registerPointerBuiltins（load 按元素类型编解码）；Sources/PiniCore/Type/TypeChecker.swift：load 调用无 unsafe 上下文门禁（与 E-110 同位点族）"
validated_at = "2026-09-04T17:40+08:00"
status = "FRESH"
note = "探针实测：/tmp/f5probe main|func 内 let v = load(p)（p: *U8）→ 正常输出 0（新分配内存清零，U8 解码），无 E4-001。工单处置：G2 加时间线注记而非改写历史观测；示例注释更新列为后续项"

[[evidence]]
id = "E-112"
assertion = "de-facto-grammar-pinning F1（算符→opText 映射表 17 项）已被 spec 钉定落地——spec 投影主题载明完整映射表并声明『自举 parser 已按此表对齐，差分门禁以之为权威参考』；工单状态行此前仍写『其余四项仍 Proposed』为滞后（矛盾 B），F2/F3/F4 经查 spec 无对应钉定，仍 Proposed"
spec_ref = "docs/spec/pini-spec-v0.md 投影主题（opText 映射表 + 自举对齐句，约 :886）；docs/spec/issue/issue-de-facto-grammar-pinning-2026-08-30.md §F1（补落地注记落点）"
code_ref = "docs/spec/pini-spec-v0.md 投影主题（文档钉定类证据，无代码变更）；自举侧对齐实现见 examples/selfhost/src/parser/parser.pini（历史交付）"
validated_at = "2026-09-04T17:40+08:00"
status = "FRESH"
note = "查证方式：spec 全文 grep opText 命中映射表与自举对齐声明；F2（logicalAnd/logicalOr/power 死面清理）/F3（前缀 ++/-- 语义）/F4（tuple-or-paren 四条）grep spec 无对应钉定条目，维持 Proposed；2026-09-04T17:55+08:00 批（工单整理·矛盾合并取证）：新增 E-110..E-112——E-110 F5 缺陷实测复现与立案（main|func 裸调 ffi_malloc 无 unsafe 不拦；门禁位点 = TypeChecker.checkExpression，foreign 调用位无 unsafeContextDepth）、E-111 ffi-module G2 语义过时实测（main 裸 load 按元素类型编解码返回 0，ADR-015 前旧观测）、E-112 pinning F1 opText 表已入 spec 钉定（矛盾 B 消解）。**本次仅新增并现跑重筛 E-110..E-112；存量条目未重新验证，按其自身 validated_at 判定。**"
