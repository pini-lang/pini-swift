# 把内联的 Pini 源码从 Swift 测试中抽离

- 状态：**已收口（2026-09-03）**——15 块内联 Pini 源码全部抽离为 `.pini` 夹具；
  不抽离的部分见下方「不抽离的裁决」。立项 2026-08-31；2026-09-02 复核发现原规模数据有误。
- **⚠️ 原「顺序约束」作废（2026-09-03）**：此前以「819 块 / 102 文件」为前提论证
  「抽离必须先于测试目录分组」。该前提经实测证伪（见下方现状复核），论证不成立。
  **测试目录分组不再受本工单阻塞**，可按自身节奏立项。
  （教训：据以排期的量化现状，开工前须实测复核——差 ~30 倍会导致整轮规划空转。）
- 动因（用户判断）：Pini 源码内联在 Swift 测试字符串里**本身就是错误的写法，极其危险**——
  已多次出现「源码含 `|` 却阻塞于 EOF」这类问题，其根因之一就是内联字符串对换行、缩进、
  转义的处理不受 Pini 自身约束。抽离后，问题从「内联字符串问题」变成「`.pini` 文件问题」，
  可以用文件级的工具与门禁处理。
- 附带收益：**已失效（2026-09-03 确认）**。H-4 已于 2026-08-31 阶段 2 直接落地
  （`docs/spec/issue/archive/issue-conflict-register-2026-08-31.md` 收敛项 H-4：宿主三路径转强制 +
  存量补冒号 1089 行 / 804 文件），**未依赖本抽离**。证据表 E-101。
  故本工单的动因现只剩首条（内联字符串不受 Pini 自身约束）。

## 现状

### 2026-09-03 实测复核（取代下表）

原表的「819 块」口径有误。实测 `Tests/PiniTests/`：

| 项 | 实测 |
|---|---|
| 三引号块 `"""..."""` | **23**（定界符共 46 次，仅 4 个文件） |
| 其中真 Pini 源码 | **15**（ParenEqualsTests 11 + IOTests 4） |
| 其中 TOML 配置（非 Pini 源码） | 8（ModuleTestCollectionTests 3 / ModuleToolchainTests 5） |
| 真·Swift 插值块 | **1**（IOTests `testAbsolutePathUnaffectedByBase`，动态路径注入） |
| 单行串中喂给 Pini 前端者 | 290；含换行者字节数仅 6–40B，均为断言期望值 |

**不抽离的裁决**（避免下一轮重新纠结）：

1. **TOML 块不抽**——不是 Pini 源码，抽了无收益。
2. **单行源码串不抽**——`Lexer(source: "x", ...)` 抽成 1 字节 `.pini` 是净损失。
3. **含换行的单行串不抽**——实测均为 `XCTAssertEqual` 的期望输出（如 `"3\n6"`），非源码。

证据：E-099（规模实测）、E-100（抽离落地与语义等价验证）。

### 2026-08-31 原表（数据有误，保留供对照）

| 项 | 数量 |
|---|---|
| Swift 测试文件 | 102 |
| 测试函数段 | 1089 |
| 多行字符串块 `"""..."""` | **819**（其中 798 块像 Pini 源码） |
| 每函数块数 | 0 块：297、1 块：784、**2 块：22、3 块：1** |
| 函数外块（共享常量 / 辅助函数内） | 18 |
| 块大小 | <200B：620、200–400B：149、400–600B：24、600B+：5 |
| **真·Swift 插值块** | **4** |
| 以换行结尾 | 819 / 819（统一，利好） |
| 位置敏感断言文件 | 3（ErrorFormatterTests / DiagnosticTests / IRGeneratorTests） |
| `Lexer(source:fileName:)` 传假名 | 209 处传 `"test.pini"` 一类 |

## 裁决（用户拍板，2026-08-31）

1. **路径机制**：`#filePath` 推导同目录（**不改 Package.swift**）。SPM `Resources` +
   `Bundle.module` 作为备选，仅在 `#filePath` 方案受阻时启用。
2. **fileName**：**继续传原假名**（`"test.pini"` 等）。位置敏感断言依赖诊断文本里的文件名，
   换真实路径会破坏那 3 个文件；抽离的收益来自内容外置，与 fileName 无关。
3. **4 处 Swift 插值**：**先逐个判定意图再动**。它们是路径注入（`\(path)`），需要动态值；
   建议改为「文件里写占位符 + 加载后替换」，或作为例外保留内联——实施前逐处确认。
4. **测试助手统一（补 check）**：**留作独立批次**。借机统一会暴露「harness 不跑 checker」
   一类真缺陷并转红（G-P8 教训），必须单独一批、按 D9 如实记录，不混进抽离批次。

## 命名与路径规则

- 目录：`<测试文件名去扩展名>/`，与 `.swift` 文件同级（如 `Tests/PiniTests/IOTests/`）。
- 文件：`<测试函数名>.pini`；同函数第 2 块起加序号 `<func>_2.pini`、`_3`……
- 函数外块（共享常量 / 辅助函数内的块）：`<_常量名>.pini`，下划线前缀以区别于函数块；
  同目录重名追加序号。
- 唯一性：以「文件 + 名字」为键校验，抽取脚本须断言路径两两不同（本次 819 块 → 819 路径）。
- 加载助手：新增 `loadSource(_ name: String, #filePath)`——从调用点所在目录读取；
  **fileName 仍传原假名**，源码内容改用文件内容。

## 保真校验（不可省略）

抽离的唯一风险是**静默改变测试语义**。因此：

1. 抽取时对每个块记录 `sha256[:16]`、字节数、是否以换行结尾、是否含 Swift 插值
   （`/tmp/pini_extract/manifest.json` 是本次基线）。
2. 抽离落地后重算哈希并与基线比对——**任何一处不同即视为失败并回滚**。
3. 缩进按 Swift 多行字符串规则剥离（剥离量 = 非空行最小前导空白，保留相对缩进），
   因为 Pini 缩进敏感。
4. 尾部换行逐字节保留（本次 819 块全部以换行结尾，落地后仍须全部以换行结尾）。

## 边缘情况与处置

| # | 情况 | 处置 |
|---|---|---|
| 1 | 多块函数（23 块） | 序号命名 `_2` / `_3` |
| 2 | 函数外块（18 块） | 按常量名命名 + `_` 前缀；重名加序号 |
| 3 | 真·Swift 插值（4 块：IOTests 三个 + IRExecutionTests 一个，均为路径注入） | 先判定意图；建议占位符替换，或例外保留内联 |
| 4 | 假插值（10 块，实为正确转义的 Pini 插值 `\\(`） | 正常抽离，无需特殊处理 |
| 5 | 单行源码字符串（非 `"""` 形态） | **2026-09-03 裁决：不抽离**。实测喂给前端者 290 个，含换行者字节数仅 6–40B 且均为断言期望值；抽离是净损失 |
| 6 | 位置敏感断言（3 文件） | fileName 保持假名规避 |
| 7 | 0 块函数（297 个，程序化构造源码） | 不受影响 |
| 8 | 测试夹具 `.pini` 与 selfhost 门禁 | 夹具**不纳入**差分门禁 |
| 9 | SPM 配置 | `#filePath` 方案无需改动；须确认测试目标未排除 `.pini` |
| 10 | **非 `throws` 测试函数**调用 `try loadPiniFixture` 编译失败（试点命中 1/5） | 给该测试函数加 `throws`（比 `try!` 诚实：加载失败应失败测试） |
| 11 | `#filePath` 的求值点 | 助手的 `filePath` 参数**不能给默认值**——默认值在定义处求值，会指向助手文件；必须由调用点显式传入 |
| 12 | SwiftPM 对测试目录下的 `.pini` | 试点未触发警告或错误；若后续出现未处理文件告警，用测试 target 的 `exclude` 消解 |

## 执行序

1. ✅ 单文件试点（GenericEnumTests，5 块，2026-08-31）：夹具目录 +
   共享助手 `Tests/PiniTests/PiniFixtureLoader.swift`（`loadPiniFixture(_:filePath:)`，
   由 `#filePath` 推导同目录）；全量 1048 测试 0 失败。
   命中边缘情况：1 个非 `throws` 测试函数需补 `throws`。
2. ✅ 规模复核（2026-09-03）：实测推翻原「819 块」口径 → 真 Pini 源码 15 块。
   原保真基线 manifest 随 `/tmp` 清理丢失，已重算并存至工作区 artifacts（不再依赖 `/tmp`）。
3. ✅ 抽离收口（2026-09-03，本批）：15 块全部落地——ParenEqualsTests 11 + IOTests 4
   （含 1 处 `__PATH__` 占位符）；3 个非 `throws` 函数补 `throws`。
   验收：目标测试 22/22、全量 **1144 tests / 0 failures / 20 skipped**。
4. **独立批次（未做）**：统一测试助手并补 check，按 D9 如实记录暴露的缺陷。

## 风险

- **静默语义变更**：验收以「测试语义等价」为准。2026-09-03 批：目标 22/22 + 全量 1144/0。
  缩进剥离按 Swift 多行字符串规则——去掉结束定界符缩进量、保留相对缩进，
  产物不以换行结尾（与既有 `IOTests/testReadFile.pini` 一致）。
- ~~**规模**：819 块 / 102 文件，必须分批，禁止一次全量替换~~ —— **已证伪**，实测 15 块。
- ~~**与 H-4 的耦合**：抽离完成后再执行冒号强制迁移，顺序不可颠倒~~
  —— **已过期**：H-4 于 2026-08-31 阶段 2 直接落地，未依赖本抽离（证据表 E-101）。
- **数据漂移（本轮教训）**：本文档原「819 块」为错误口径，据其做出的
  「分批 10–20 个文件推进 / 抽离先于目录分组」规划全部作废。
  **凡本文件中用于排期的数字，引用前须实测复核。**
