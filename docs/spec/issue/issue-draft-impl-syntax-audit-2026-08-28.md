# 工单：Pini草稿.md ↔ 实现语法审计（反录入演进偏差）

- 日期：2026-08-28
- 提出方：agent:pini-dev（用户委托："草稿因反录入演进过多，可能存在与实现不吻合的其他语法，找出来"）
- 方法：以 `Pini草稿.md`（设计原意）逐条提取语法断言 → 宿主 v0.48.4+（main 0d5bf64）`pini check` / `pini tokens` / `pini run` 最小样本探针（四轮，约 70 样本）；错误按 E2（解析拒）/ E3+（语义收，语法通）分类。
- 状态：审计完成，处置待拍板

## A. 草稿与实现冲突（实现为准 → 草稿需订正；或登记宿主收敛项）

| # | 草稿位置 | 草稿说 | 实现实证 |
|---|---|---|---|
| A1 | 行235-236 `capture 标识符` / 行452 示例 | "匿名函数**体内部**列出需捕获的外部标识符" | 实现是 **签名前**：`func capture o () -> ():` ✓；体内部 `capture o` **E2-006 拒**（Parser.swift:1171 起） |
| A2 | 行244 捕获规则表 | 本身方法"允许捕获 self" | `func capture self () -> ():` **E2-001 拒**（capture 列表仅收 IDENT，`self` 是关键字） |
| A3 | 行199 内建函数清单 | 列 `contains`/`upper`/`lower`/`split`/`substring` 为**自由函数** | 自由函数形态 **E3-002 undefined**；实现是 **String 成员方法**（`"a".contains("b")` ✓ / `.upper()` ✓ / `.substring(0,2,)` ✓ / `.split(",")` ✓） |
| A4 | 行199 内建函数清单 | 含 `cancel`、`join` | 宿主注册表无 `cancel`/`join`（有 `isCancel` / `joinAll` / `joinWithin`） |
| A5 | 行199 内建函数清单 | 18+2 清单 | 宿主实际：print/len/abs/min/max/sqrt/**sin/cos/tan**/writeFile/readFile/readLine/sleep/**is_letter**/**assert**/msg/ok/err/Error/CancelError/isCancel/joinAll/joinWithin/**load/store/addressof**——双向出入（草稿缺三角函数、is_letter、assert、FFI 指针原语） |
| A6 | 行216 缺省参数 | "缺省参数是具值的元组元素，集中后置"：`f|func(a: I32, b: I32 = 5,)` | **E2-001 拒**（默认参数未实现） |
| A7 | 行174-175 单类型返回 | `函数块(参数,)->返回类型`（无元组括号） | **E2-001 拒**；`-> (I32)`（单元素元组）OK |
| A8 | 行437/450 示例 | `安全分配(大小: I64,) -> ^*I8:` 函数签名**带 `:` 开块** | `add|func(a: I32,) -> (I32):` **E2-007 拒**；无冒号形态 ✓（草稿内部不一致：行408 无冒号、行437/450 有冒号） |
| A9 | 行120 结构块泛型 | `(结构块)<泛型:约束,>`（冒号分隔约束） | **E2-001 拒**；`\|` 分隔 ✓（与草稿行203 一致 —— 草稿自身前后矛盾） |
| A10 | 行49 match 解构 | `case 形状.圆(_, 半径):`（限定名解构） | **E2-001 拒**；非限定 `case 圆(半径):` ✓ |
| A11 | 行39 扩展块 | `[[扩展枚举块:可选的特征约束]]`（枚举扩展带冒号约束） | **E2-005 拒**；`[[形状]]` ✓、`((点:大小))` ✓、`{{盒:大小}}` ✓（仅枚举扩展侧不支持） |
| A12 | 行4/76-84 集合多行 | "遇到集合语法（如元组）允许多行" | 跨行元组/数组字面量 **E2-006 拒**（单行必须写完） |
| A13 | 行52 路径枚举 | `[路径枚举\|关键字]` | `[路径\|path]` **E2-005 拒**；`[路径\|enum]` ✓（无 path 关键字） |
| A14 | 行248 测试函数 | "编译器收集所有测试函数并在**入口依次调用**" | `pini run` 仅输出 MAIN、不执行 `\|test`；收集执行在 `pini test` 子命令（G41 → G49 收为模块单位）——设计已演进 |

## B. 与 G51 重叠项（已裁决，见 issue-spec-impl-syntax-audit-2026-08-28）

- import/export：草稿块形式（行54-74）与 spec 一致，宿主裸语句为偏差 → 宿主收敛待办；
- defer：草稿块形式（行343）→ spec 已改双形态，宿主待实现块形式；
- 花括号函数声明：草稿无此设计（行167-172 函数块仅裸名）→ spec 已删除。

## C. 草稿内部自相矛盾（不依赖实现即可判定）

- C1 泛型约束分隔符：行120 用 `:`，行203 用 `|`；
- C2 函数签名开块冒号：行408 无冒号，行437/450 有冒号；
- C3 `defer` 形态：行343 块形式，行423 示例 `defer unsafe free(p)` 无冒号。

## D. 一致项抽查（草稿 ↔ 实现吻合）

`;`/`#` 注释、`\(...)` 插值、`_` 访问控制前缀语义、`[枚举块|enum]` 与缺省枚举、`[X|struct]` 拒（草稿行102 明示未提供）、`a[i]` 下标、`<特征块名>` + `|self`/`|own` 方法、`<<扩展特征块>>` 默认实现、`own` 自指语义、`(结构块)`+组合语法、`((扩展结构块:约束))`、`{{扩展对象块:约束}}`、Optional `.some/.none/nil/?T`、WeakRef/LazyRef、集合字面量（单行）、`^T` 与 `^右值`、main 特殊标识符、`main() -> ()` 无 `|func` 顶级、泛型函数 `<T | 约束>`、异步 `=>` + `await`/`wait`/`detach`、匿名函数四形态 + IIFE + `wait 消费`、`|unsafe` 自由函数与安全封装、指针字段（object/struct 均可含 `*T`）、`unsafe (expr)` 括号复合、`|self|unsafe`/`|test|unsafe` 组合被禁、运算符全集、控制块同行表达式替换、`step`、`for (模式元组,) in`、`main` 入口、`main` 退出码。

## E. 待查（探针未能判定）

- `throw err(...)` + `try/except` 同步路径：`-> (^I32,)` 函数内 throw 报 E3-001（异步 CPS 路径有测试覆盖）；草稿行95 "属于 Error 的字面量适用于 try、except" 的语义边界待核实。

## F. 草稿复检（用户修订后，2026-08-28 18:36）

用户修订集中在 FFI/unsafe 段与泛型约束分隔符。识别到的改动：
1. 行423-424 `defer unsafe free(p)` → **`defer:` 块形式**；
2. 行438 安全分配示例加 `|unsafe` 修饰符，行439 去掉内部 `unsafe` 前缀（`|unsafe` 上下文内直调 C 函数）；
3. 行451 写入内存示例**去掉开块冒号**；
4. 行203 泛型约束分隔符 `|` → **`:`**（与行120 统一）。

### F1 需裁决：泛型约束分隔符（草稿 vs spec/实现，方向相反）

草稿行120+203 现已统一为 `<T:约束>`；但 **spec EBNF `generic-params ::= IDENT ['|' type-annotation]`** 与宿主实现**均只认 `|`**：

- `f|func<T: 大小>(v: T)` → E2-001 拒；`(盒<T:大小>)` → E2-001 拒
- `f|func<T | 大小>(v: T)` → OK

原 C1 内部矛盾已消除，但方向与 spec/实现相反。二选一：
- **(a) 草稿改回 `|`**（推荐：spec 为权威事实源，改动面最小）；
- (b) spec 变更 + 宿主收敛到 `:`。

### F2 新内部矛盾：函数开块冒号

行438 `安全分配|unsafe(大小: I64,) -> ^*I8:` 带冒号；行451 `写入内存|unsafe(目标: *I8, 数据: [I32],) -> ()` 无冒号。宿主**只收无冒号**（带冒号 E2-007 拒）。建议草稿统一为无冒号（或明确"冒号为可选尾缀"并登记实现缺口）。

### F3 新发现：foreign 签名不接受 `|unsafe` 修饰符

行435-436 `malloc|unsafe(大小: I64) -> *I8` → **E2-001 拒**；无修饰符形式 OK（foreign 块内函数本就自动 unsafe，行429 已述——修饰符冗余）。

### F4 新发现：点前缀枚举字面量全部被拒

草稿行150-151 `.some(值)`/`.none`、行178-179/441/479/481 `.ok(值)`/`.err(错)` → **均 E2-006 拒**。实现用裸内建/构造：`ok(1)` ✓、`err("bad")` ✓；Optional 侧裸 `some(1)`/`none` 亦不存在（E3-002/E3-001），Optional 由下标读/类型系统产出（G48 严格枚举）。草稿 Result/Optional 两节的字面量记法需订正。

### F5 新发现：安全上下文调用 foreign 无门禁

行430"安全上下文中需要 `unsafe` 消耗点"——实测 `main|func` 内 `let p = malloc(16)` 无前缀：**check 通过且 `pini run` 正常运行输出**（静态层与运行时均不拦）。实现未强制该规则（或门禁尚未落地）。

### F6 已登记：defer 块形式

行423 块形式仍 E2-006 拒（相邻形式 OK）——与 G51 裁决一致，宿主收敛待办。

### 未改动项提醒（A 级仍在）

capture 位置与 `capture self`、内建函数清单双向出入、默认参数、单类型返回糖、限定解构 `case 形状.圆(...)`、枚举扩展冒号约束、跨行集合字面量、`[路径枚举|关键字]`、`|test` 不在程序入口被收集调用。

## 处置选项（待拍板）

1. **草稿订正**（spec 为权威事实源，草稿作为设计原意文档随 G 号同步修订）：A1-A13 逐条按实现/spec 现状订正，C1-C3 消歧；
2. **实现收敛**（若某条草稿意图更强）：A6 默认参数、A7 单返回糖、A8 冒号开块、A10 限定解构、A11 枚举扩展约束、A12 跨行集合、A13 路径枚举——按优先级登记宿主工单；
3. **spec 连带订正**：spec §813 capture 注释（"体内部"）复制了草稿错误，应随 A1 一并改为"签名前 `func capture 标识符 (参数,) -> ():`"；
4. **草稿入库**：Pini草稿.md 目前**未跟踪**——若作为 G 号引用事实源（G51 已引用其 import/特征块/对象块段落），建议纳入 pini-meta 版本管理。
