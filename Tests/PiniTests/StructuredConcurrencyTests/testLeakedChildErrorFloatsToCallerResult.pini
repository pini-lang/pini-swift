child|func() => (I32,):
    return err(Error("boom"))

worker|func() => (I32,):
    child()          ; 派发子任务但从不 `<=` join
    sleep(50)        ; 留出子任务失败的时间窗
    return ok(42)

main|func() -> ():
    var r = wait worker()
    print(r)
    return