child|func() => (I32,):
    return err(Error("boom"))

worker|func() => (I32,):
    var c = child()
    detach c        ; 显式退出 scope 所有权（语句形式，任务 #13）
    sleep(50)
    return ok(42)

main|func() -> ():
    var r = wait worker()
    print(r)
    return