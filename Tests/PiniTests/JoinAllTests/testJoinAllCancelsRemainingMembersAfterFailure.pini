bad|func() => (I32,):
    return err(Error("先失败"))

slow|func() => (I32,):
    sleep(3000)
    print("慢成员不该跑完")
    return ok(9)

main|func() -> ():
    var b = bad()
    var s = slow()
    var r = wait joinAll([b, s])
    match r:
        case ok(vs):
            print("不应成功")
        case err(e):
            print(e)
    sleep(200)
    print("主流程结束")
    return