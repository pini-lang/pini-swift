main() -> ():
    var a = Optional.some(42)
    match a:
        case some(v,):
            print(v)
        case none:
            print("none")
    var b = Optional.none
    match b:
        case some(v,):
            print(v)
        case none:
            print("none")
    return