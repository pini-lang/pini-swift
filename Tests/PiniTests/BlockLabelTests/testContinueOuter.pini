main|func() -> ():
    var count = 0
    outer|while true:
        count = count + 1
        if count >= 3:
            break
        while true:
            continue outer
            print("inner")
    print(count)
    return