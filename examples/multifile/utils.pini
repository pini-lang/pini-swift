; 多文件模块：本文件定义的结构与函数被 main.pini 跨文件使用
; 运行：pini run examples/multifile   （与 main.pini 同属一个模块）
(向量)
x: I32 = 0
y: I32 = 0

add|func(a: I32, b: I32) -> (I32)
    return a + b

square|func(n: I32) -> (I32)
    return n * n
