# Pini 多文件模块清单（P4 Phase 5 演示）
# 本目录含 pini.toml，故 CLI 将其视为「一个显式多文件模块」而非独立文件集合。
[package]
name = "demo"
version = "0.1.0"

# 依赖通道（批 6，G52）：模块依赖用 [require]（由代码中的 import 生成，pini mod tidy 对齐），
# 非 Pini 资源用 [resources]；旧 [dependencies] 节已移除，命中即报错。
