; 元组：多返回值分组与整体传参
; 运行：pini run examples/tuple.pini
; 预期：[3, 2]
;
; 说明：元组用 (a, b) 构造，可作多返回值整体返回、整体作为实参传递、整体打印。
;       元素访问 .0 / .名称 与解构 var (a, b) = 已支持，
;       见 spec 元组解构/元素访问主题（此处示例保持整体使用以演示多返回值）。

除余|func(a: I32, b: I32) -> ((I32, I32,),):
    return (a / b, a % b)

main|func() -> ():
    let r = 除余(17, 5)
    print(r)
    return
