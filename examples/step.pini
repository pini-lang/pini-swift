; 控制流 while step：每轮循环体结束后执行的步进块（类 C for 的步进子句）
; 运行：pini run examples/step.pini
; 预期：
;   0
;   step
;   1
;   step
;   2
;   step
;   0
;   步进块
;   1
;   步进块
;   2
;
; 说明：
;   - step: 紧跟 while 的缩进块，在每轮循环体正常结束后执行一次。
;   - 循环体内 continue 仍会触发 step；break 直接跳出，跳过 step。

main|func() -> ():
    ; 基础：step 在每轮后执行
    var i = 0
    while i < 3:
        print(i)
        i = i + 1
    step:
        print("step")

    ; break 跳过 step：k 到 2 时直接跳出，不打印"步进块"
    var k = 0
    while k < 5:
        print(k)
        k = k + 1
        if k == 3:
            break
    step:
        print("步进块")
    return
